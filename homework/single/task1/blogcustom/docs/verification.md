# 验证记录

日期：2026-09-08。当前版本将统计统一放到页脚，顶栏搜索靠右 4px，侧栏缩至 220px，保留 28px 文章标题、22px 公共角饰、单行顶栏、径向设置与手机搜索。使用桌面 Chrome 和应用内浏览器验证；窄屏为模拟视口，没有等同于真实手机实机测试。

## 当前版本实测：页脚统计、右侧搜索与侧栏宽度

- 修改前后台 CSS、页首与页脚全文均与本地一致；备份到 `backup/before-footer-stats-20260908/`。CSS 与页首修改后全文回读一致再保存，公告换行修正后的最终 CSS 也已核对保存。
- 首页 320、390、767、768、1024、1100、1440、1920px：顶栏均高 54px，标题到左内边缘 16px，搜索控件到右/上/下内边缘均为 4px。无标题搜索重叠、无页面横向溢出。
- 上述八档视口均仅一个 `.blogStats`，父节点始终为 `#footer` 且可见；由手机放大到桌面也不再移回顶栏。原平台统计数字继续显示，未复制节点或写死数据。
- ≥768px 侧栏内容宽与资料卡宽均为 220px，比原 230px 少 10px；主栏左预留为 254px，比原 264px 少 10px。资料卡到正文列间距为 29px；两枚统计框各宽 93px，间隔 4px。≤767px 侧栏仍隐藏。
- 公告的 `word-break` 为 normal；用文本 Range 测量“2601”只有一个文字矩形，数字未被拆行。已检查桌面资料卡、两枚统计框与角饰实际截图。
- 390px 手机点击搜索按钮后，输入框正常浮出并聚焦，顶栏仍高 54px；Escape 收起并返回按钮，统计保持在页脚。
- 文章页初始以 320px 打开，再检查 320、768、1440px：统计唯一且位于页脚、顶栏高 54px、右空隙 4px，页面和四个图片外框无横向溢出。
- [桌面顶栏与侧栏](screenshots/header-right-search-desktop-20260908.png)、[手机搜索与页脚](screenshots/header-right-search-mobile-20260908.png)。本地 CSS、独立 JS、生成 HTML、README 与实现/选择器文档已同步；JS 语法、HTML 生成与差异空白检查通过。

## 历史版本实测：22px 公共角饰（2026-09-07）

- 按两步完成：先将原资料卡的双斜线覆盖到资料卡、两枚统计按钮和四张正文图片，线上检查七处全部存在；随后合并为公共 `.blog-outline-card::after`，最终按反馈去掉最贴近圆弧的一条线，保留靠外两条 0.8px 线与 3px 三角。
- 首页与文章页分别检查 320、390、768、1100、1440px：没有遗漏的 22px 圆角组件，没有页面横向溢出；顶栏仍高 54px，内边距仍为 4px 16px。
- 装饰右、下偏移与卡片边框相抵，伪元素外边界与组件外边界误差为 0；正文图片外框与图像高度差为 0，宽度差不超过 0.032px（浏览器小数像素取整）。首页摘要图外框与原图宽高一致，保持 135px 宽、右浮动。
- 首页四处角饰（含摘要图）与文章七处角饰共用相同 SVG mask。亮色为黑色，暗色为 `rgb(227,229,232)`，全部 `pointer-events:none`。最后删除一条绘制路径后重新读取线上 mask，确认只剩两条斜线。
- 统计链接键盘聚焦后圆角仍为 22px。圆形头像仍为 50%，正文 favicon 仍为 0px 圆角，21 个 favicon 保留。
- 正文四张图片 URL、22px 四角裁切、1px 边框保留，亮色桌面和暗色手机均看过实际截图。图片点击放大与 Escape 收起正常，始终四个外框、零重复嵌套，没有新增横向溢出。
- [桌面两线角饰](screenshots/outline-corners-desktop.png)、[手机暗色图片角饰](screenshots/outline-corners-mobile-dark.png)。图片包装脚本、生成器、CSS、两份 HTML 与 README/实现说明/选择器清单已同步；改造前代码在 `backup/before-shared-corners-20260907/`。
- 后台 CSS、页首与页脚通过编辑器全文回读核对后保存，最终两线 CSS 再次回读一致并验证线上生效。JS 语法检查、HTML 生成及差异空白检查通过。

## 历史版本实测：16px 间距与文章图像

- 修改前复制后台 CSS、页首、页脚全文；CSS 与本轮备份一致，页首/页脚与本地文件一致。新 CSS 粘贴后全文回读一致再保存。
- 当前平台 Markdown H1 原规则为 28px、bold、1.5 行高；新文章标题实测 28px、42px 行高。
- 320、390、768、1100、1440px：顶栏高 54px、上下空隙 4px、标题左侧 16px；桌面统计/手机搜索按钮右侧 16px。文章标题无内容裁切，页面及四张正文图片无横向溢出。
- 四张正文图均为 22px 圆角与 1px 描边；桌面亮色和手机暗色实图已检查四个角，原图 URL 保留，缩放随容器并保持比例。
- 圆形头像仍为 50% 圆角；抽查正文 favicon 为 0px 边框、0px 圆角。平台图片点击放大并收起正常；首页摘要图同样为 22px 圆角、1px 描边，标题为 28px。
- [手机图片圆角截图](screenshots/article-images-dark-mobile.png)、[文章标题截图](screenshots/article-title-mobile.png)。本轮 CSS 回滚文件在 backup/before-article-outline-20260907/。
- 本次会话所有现行行为已合并到 README 的“当前功能与源码”，旧 4/8px 与列表菜单仅作为历史记录。JS 与生成 HTML 分开维护，构建脚本保持一致。

## 历史版本实测：单行顶栏与径向图标

- CSS 和页首脚本修改前已与线上全文比对并备份；新内容粘贴后全文回读一致，再保存。
- 320、390、767、768、1024、1100、1440、1920px：顶栏均高 54px，无页面横向溢出或标题与搜索重叠。
- 320px 文章页顶栏同为 54px、无页面横向溢出；用户原 Chrome 文章页已刷新确认新版生效。
- 每档实测搜索控件到外壳内边缘上下各 4px，标题左侧 8px；有顶栏统计时其右侧 8px，手机搜索按钮右侧 8px。测量排除外壳本身的 1px 边框。
- 标题和统计边框计算值为 0px；更多展开后只有两个 44px 圆形操作，中心距 72px，实测角度约 15.000° 和 75.000°，外层无边框。
- 手机点击搜索后表单浮出并聚焦输入框，顶栏仍高 54px；Escape 收起并返回按钮。搜索按钮提交得到正确的 w=blog:155TuT 软件工程 参数。
- 配色切换正常；刷新后恢复与当前浏览器一致的默认配色。跟随选项及状态节点均不存在。
- 截图见 [手机径向按钮](screenshots/mobile-radial-tools.png)。回滚文件为 backup/before-radial-tools-20260907/ 下的 CSS 与页首 HTML。
- JS 语法、HTML 生成和差异空白检查通过。窄屏为浏览器模拟，未改操作系统配色，也未实测手机 Safari。

## 历史版本实测：浮动设置与明暗配色

- 本地备份 CSS、页首 HTML 与修改前后台复制全文逐字一致；新代码粘贴后全文回读一致再保存。后续修正了页脚 float 和日期颜色。
- 首页 320、390、767、768、1024、1100、1440、1920px：无页面横向溢出，无顶栏控件重叠；≤767px 高 124px，其余高 66px。
- 统计原节点在 ≤1099px 位于 footer，≥1100px 位于 navigator；实测连续跨断点正确移动，没有重复。
- 六个导航按钮从 DOM 删除，更多设置每页仅有一个；手机文章页 390px 无横向溢出，原正文 21 个 favicon 装饰仍存在。
- 浮动菜单展开、明暗切换、手动暗色刷新记忆、恢复跟随、Escape 收起和焦点返回、点击外部收起通过；默认配色与当前浏览器偏好一致。
- 已看过首页亮色/暗色、手机文章页暗色表格和正文；没有给图片添加反色滤镜。
- Enter 搜索仍提交正确的 w=blog:155TuT 软件工程；到达找找看人机验证页，未执行验证码，未将搜索结果计数当成检索成功。
- JS 语法检查与生成页首/页脚通过。系统偏好变化监听已实现，但未改变用户的操作系统配色做端到端验证；未实测 Safari/真机触控和所有代码高亮语言。
- 回滚：backup/before-floating-settings-20260907/ 下的 CSS 和页首 HTML 同时恢复。

前一版本手机效果：[暗色菜单截图](screenshots/mobile-outline-dark.png)。

## 历史部署与资源检查

- 页面 CSS、页首 HTML、页脚 HTML 已分别保存至博客后台，并与本地文件及编辑器全文核对。
- Custom 默认 CSS 保持启用；后台日历和找找看关闭，公告保留。
- 字体 CSS、原始 WOFF2 和四份许可证逐一对照原文件，SHA256 一致，详见 [字体说明](../fonts/README.md)。
- 页首脚本位于导航结构之前；默认搜索图标为内嵌 SVG，显式包含 20×20 的几何尺寸属性，不需要外部图片请求。
- 已核对后台「文件」页允许上传 SVG，但允许列表不含 PNG/WebP；本轮未实际上传图标文件。

## 历史：紧凑顶栏与公告版本

本轮更改 CSS 和页首脚本，页脚 favicon 没有变化。回滚文件为 `backup/before-compact-header.css` 与 `backup/before-announcement-header.html`。

| 场景 | 实际结果 |
|---|---|
| CSS 与页首部署 | 已部署当前代码，包含最后的手机边界微调 |
| Chrome 2504px 紧凑布局 | 顶栏高 54px，外壳内边距为 4px，六个导航按钮均宽 54px |
| 文章页八档 | 360、390、768、1024、1300、1366、1800、1920px 均检查通过：外壳四周内边距为 4px，六个按钮在同一断点等宽，无页面横向溢出 |
| 按钮尺寸 | ≤1299px 按钮宽 44px，≥1300px 宽 54px；普通控件与卡片圆角为 22px，双斜线偏移为右下各 2px |
| 搜索位置 | 上述文章页各档搜索中心与外壳中心误差不超过 0.01px |
| 圆弧边界 | 按各控件实际圆角弧采样，均位于外层胶囊内；没有将圆角裁去的矩形四角误当成控件边界 |
| 顶栏高度 | 360、390px 为 198px；768、1024、1300、1366px 为 102px；1800、1920px 为 54px；Chrome 2504px 同为 54px |
| 首页 1366px | 实图与结构检查通过，顶栏高 102px |
| 资料卡公告 | 每页公告 ID 仅有一份，父节点为 `#blog-profile-card`，自身计算显示为 `block`，原文字和 GitHub 链接保留 |
| 粉丝与关注 | 两列分别将标签与数字合为一个整体，各有 1px 描边 |
| 手机侧栏 | 侧栏为隐藏状态；公告节点仍保留在隐藏的资料卡内，不会出现在主内容区域 |
| 手机边界微调 | 左组向左平移 8px，统计组左右内边距各 6px、项目间距 4px，360px 视口未擦过外壳边缘 |

此前的 96px / 314px 顶栏高度属于增加紧凑间距前的布局，不能作为当前结果。下方保留较早阶段的历史测试，不以旧版通过推定本轮通过。公告的原节点和链接保留已验证，本轮没有人为模拟反复异步替换，也没有执行冷缓存录屏或 CLS 量化。

## 前次胶囊顶栏版本的历史结果

此阶段使用标题在导航上方、统计两列、斜线位于控件内部的布局，已备份到 `backup/before-horizontal-header.css`。以下结果为该阶段的历史验证。

| 场景 | 实际结果 |
|---|---|
| 本地与线上一致 | 胶囊版 CSS 与页首 HTML 均与本地全文匹配；页脚 favicon 本轮没有更改 |
| 桌面 70% 宽度 | Chrome 2504px 窗口下页面可用宽度为 2489px，`#home` 为约 1742.3px，比例为 70% |
| 胶囊与搜索 | 外壳及搜索的圆角均为 999px；桌面搜索中心与外壳中心实测误差约 0.008px |
| 原生节点 | `#blogTitle` 已移入 `#blog-header-left`；统计组确认为四个直接子 `span` |
| 名字与签名 | 已清除原皮肤 `h1/h2` 的左浮动及 50% 宽度，两行内容完整 |
| 390px 手机视口 | 截图中各控件和双斜线可见，外壳为竖向胶囊，页面左右 12px 外边距保留 |
| 首页八档宽度 | 360、390、768、1024、1120、1300、1366、1920px 均检查通过；搜索仅有一个，中心误差不超过 0.01px，无页面横向溢出 |
| 首页圆弧边界 | 上述八档中，标题卡、六个导航按钮、搜索框和统计组各自的四角全部位于外层胶囊轮廓内 |
| 首页内容宽度 | 横屏且 ≥1024px 时 `#home` 为根元素客户宽度的 70%；≤767px 时左右各 12px，侧栏隐藏 |
| 文章页几何 | 1366×900、360×800 均通过：搜索唯一，无页面横向溢出，各控件四角位于外壳内，搜索中心误差不超过 0.01px |
| 文章页截图 | 768×1024 实际页面截图已检查 |
| 手机图标搜索 | 点击放大镜提交“软件工程 & CSS”，本站范围与 `&` 参数编码正确；返回后仍只有一个搜索框 |
| 旧功能与样式分离 | 文章正文 favicon 为 21 个，正文范围外为 0；自建组件带行内 `style` 的节点为 0 |
| 最终后台同步 | 标题浮动修复后的后台 CSS 已全文复制并与最终源码核对一致；页首 HTML 同步完成 |
| 当前个人资料 | 实测粉丝 2、关注 15，由原生资料动态读取；该数值不是代码常量 |

上述八档为首页的几何、溢出和圆弧边界检查，文章页按表内尺寸单独验证。手机放大镜实际导航至[本站搜索结果地址](https://zzkx.cnblogs.com/s?w=blog%3A155TuT+%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B+%26+CSS)；这确认了提交和编码，未绕过或完成结果页的人机验证。双斜线仅为装饰，不是可操作的缩放手柄。最终截图和宽度测量不等于冷加载零闪烁或 CLS 已量化。

## 上一版独立搜索与描边导航的在线结果

以下为 `backup/before-capsule.css` / `backup/before-capsule-header.html` 对应的历史基线。表中的 40px 搜索高度、透明顶栏布局属于上一版；当前胶囊版搜索统一为 44px。

| 场景 | 实际结果 |
|---|---|
| Chrome 2504px 随笔 | 顶栏背景透明，导航链接各有 1px 描边和 10px 圆角；搜索高 40px，按钮只有放大镜 SVG |
| 个人卡片 | 卡片高 260px，外层公告区域约 280px（含原有内边距），实际嵌套没有产生两份占位；粉丝 2、关注 14 与当时原生资料对应 |
| 页首执行位置 | 提前初始化脚本在导航 HTML 之前，不再依赖页脚等待侧栏 |
| 390px 首页 | 六个导航按钮按三列两行显示，每个高 44px；搜索高 44px；侧栏隐藏；无页面横向溢出 |
| 390px 多页面 | 随笔、分类、月份归档、阅读排行页分别存在 1 个独立搜索框，旧搜索控件为 0，无页面横向溢出 |
| 360px 随笔 | 侧栏隐藏，搜索高 44px，表单数量为 1，无页面横向溢出 |
| 768px、1366px 随笔 | 侧栏恢复，搜索高 40px，表单数量为 1，无页面横向溢出 |
| 关闭找找看 | 后台找找看关闭后，以上页面的独立搜索仍存在 |
| 空白搜索 | 空白关键词不会跳转 |
| Enter 搜索 | 输入“软件工程 & CSS”后正确导航到找找看，参数保留本站范围并正确编码特殊字符；结果页要求验证 |
| 图标点击搜索 | 点击放大镜实际提交 GET 查询，参数为 `blog:155TuT 软件工程`；与 Enter 使用同一表单机制 |
| 返回博客 | 返回后搜索仍为 1 个，图标几何尺寸仍为 20×20；用户 Chrome 首页和随笔刷新后均显示新表单且无旧搜索 |
| 正文 favicon | 当前随笔保留 21 个正文图标，正文范围外为 0 |
| 样式分离 | 新增组件没有行内 `style` 属性；两份生成 HTML 不承担 CSS 定义 |

实际检查包括博客首页、随笔详情、分类 2531560、2026 年 9 月归档和阅读排行。当前改动作用于共享 Custom `#navigator` 的博客页面，没有修改博客园后台、平台首页或独立搜索结果页；已检查的页面类型也不等于平台所有页面类型均已测试。

## 搜索与加载验证边界

当前表单通过 GET 提交隐藏字段 `w`，Enter 实测内容为 `blog:155TuT 软件工程 & CSS`，图标点击实测内容为 `blog:155TuT 软件工程`。结果页要求人机验证，本次没有操作验证码；这里只确认提交事件、博客范围和参数编码，未验证结果列表可获取。

没有录屏、CLS 数值采集、冷缓存录屏或冷网节流实验。提前执行和预留空间针对已知的动态组件后移，不能由最终截图推导为绝对消除闪烁。外部字体 CSS 的 `@import` 和 `font-display: swap` 保留，因此慢网或冷缓存仍可能发生字体替换。

## 本地检查

- 当前两份独立脚本通过 `node --check` 语法检查。
- `page-header.html` 和 `page-footer.html` 分别与对应源码一致，且没有 `<style>` 或行内 `style` 属性。
- 字体资源与随包许可证完整保留，没有为本轮导航更改重新生成字体或修改 `font-display`。

## 前一轮结果与回滚基线

第一轮侧栏版本使用原生 `#widget_my_zzk`，移动至顶栏，窄屏搜索高 42px；这些实现已由本轮独立搜索替代，备份为 `backup/before-outline.css` 与 `backup/before-outline-footer.html`。不要将该阶段的 `#q`、`#btnZzk` 当作当前搜索选择器。

此前已完成的检查包括：

- 圆形头像 96px，昵称“少年漫症候群”，粉丝 2、关注 14，与当时原生资料对应；独立未登录访问也显示作者资料。
- 360px、390px、767px 窄屏隐藏侧栏，768px 恢复；页面根元素 `scrollWidth` 与 `clientWidth` 相等。
- 正文 21 个 favicon 中 19 个加载成功、2 个使用通用图标；3 个博客园链接均加载官方 SVG，正文范围外为 0；图标实测 12×12px，与链接字号相同。
- 字体视觉和计算后的 `font-family` 保留 Fusion Pixel；初次异步侧栏资料可到达并显示。

这些记录作为历史基线保留，未模拟后续计数变化或多次侧栏刷新。粉丝、关注数字是当时页面读数，当前版本在新数据到达前显示 `—`，不会把历史数字作为常量写入。

## 后续复查

更新后重新生成页首和页脚，保存三处后台代码。检查首页、随笔和一个列表页的表单数量；分别测试 Enter 与图标按钮，使用含 `&` 的关键词确认没有拆成额外参数；缩窄至 390px 再放宽至 768px 检查断点恢复。正文图标需滚动进入可视区域后再检查加载状态。

当前没有在真实手机、全部浏览器、所有皮肤、相册及全部分页状态上测试，也未主动制造字体网络超时或模拟后续粉丝数更新。新增测试应记录实际环境和结果，不将设计目标写成已验证结论。
